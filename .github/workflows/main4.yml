name: clouding 4

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Direct"
          netsh advfirewall firewall add rule name="RDP-Direct" `
            dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          $password = "WinRDP@2025"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (-not (Get-LocalUser -Name "xuantrieu" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "xuantrieu" -Password $securePass -AccountNeverExpires
          }
          Add-LocalGroupMember -Group "Administrators" -Member "xuantrieu"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "xuantrieu"
          echo "RDP_CREDS=User: xuantrieu | Password: $password" >> $env:GITHUB_ENV

      - name: Install Steam
        run: |
          $steamUrl = "https://cdn.cloudflare.steamstatic.com/client/installer/SteamSetup.exe"
          $installerPath = "$env:TEMP\SteamSetup.exe"
          Invoke-WebRequest -Uri $steamUrl -OutFile $installerPath
          Start-Process -FilePath $installerPath -ArgumentList "/S" -Wait
          Remove-Item $installerPath -Force

      - name: Add Steam to Startup
        run: |
          $startupPath = "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup"
          $steamExe = "C:\Program Files (x86)\Steam\Steam.exe"
          $shell = New-Object -ComObject WScript.Shell
          if (Test-Path $steamExe) {
              $steamShortcut = "$startupPath\Steam.lnk"
              $shortcut = $shell.CreateShortcut($steamShortcut)
              $shortcut.TargetPath = $steamExe
              $shortcut.Save()
          }

           - name: Maintain Connection
        run: |
          $ip = (Invoke-RestMethod -Uri "https://api.ipify.org?format=text")
          $hostname = $env:COMPUTERNAME
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $ip"
          Write-Host "Hostname: $hostname"
          Write-Host "Username: xuantrieu"
          Write-Host "Password: WinRDP@2025"
          Write-Host "==================`n"
          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
              Start-Sleep -Seconds 300
          }
